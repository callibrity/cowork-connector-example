name: Release native image

on:
  release:
    types: [created]

jobs:
  native-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    env:
      IMAGE: ghcr.io/${{ github.repository }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set pom version to tag
        run: mvn --batch-mode versions:set -DnewVersion=${GITHUB_REF_NAME} -DgenerateBackupPoms=false

      - name: Build native image via buildpacks
        run: |
          mvn --batch-mode -Pnative spring-boot:build-image -DskipTests \
            -Dspring-boot.build-image.imageName=${IMAGE}:${GITHUB_REF_NAME} \
            -Dspring-boot.build-image.env.BP_NATIVE_IMAGE=true \
            -Dspring-boot.build-image.env.BP_JVM_VERSION=25

      - name: Tag as latest
        run: docker tag ${IMAGE}:${GITHUB_REF_NAME} ${IMAGE}:latest

      - name: Push image
        run: |
          docker push ${IMAGE}:${GITHUB_REF_NAME}
          docker push ${IMAGE}:latest
